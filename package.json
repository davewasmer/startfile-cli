{
  "name": "start-cli",
  "version": "1.0.0",
  "description": "Run multiple long-running commands based on .start files",
  "bin": {
    "start": "index.js"
  },
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Dave Wasmer",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^3.5.1",
    "find-up": "^2.1.0"
  },
  "devDependencies": {
    "semantic-release": "^15.6.0"
  }
}
